import { View } from "react-native";
import CompletionScreen from "../src/screens/CompletionScreen";

export default function JudgeCompletion() { 
    return (
        <View style={{ flex: 1 }}>
            <CompletionScreen />
        </View>
    );
}